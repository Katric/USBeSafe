# USBeSafe CLI

## Installation
Install uv:
```bash
wget -qO- https://astral.sh/uv/install.sh | sh
```


```bash
# Install with uv
uv venv
uv pip install -e .
```
Nach der Installation liegt das executable in `.venv/bin/usbesafe`
## Usage

```bash
# Start the host daemon
usbesafe  start
```
